# stream_blockCBC_cipher
implementation of the stream cipher, a simulation of the one-time pad by using a keystream generator, and a block (16-byte) encryption with cipher block chaining and padding. 
